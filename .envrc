## API

# Local port where API container expose itself.
export API_EXPOSE_PORT=8080

## PostgreSQL
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASS="password"
export DB_NAME=postgres
export DB_EXPOSE_PORT=5432
export SERVER_IP=0.0.0.0
export SERVER_PORT=8000

## Redis
export REDIS_HOST=localhost
export REDIS_PORT=6379
export REDIS_EXPOSE_PORT=6379
## JWT

export JWT_ACCESS_PUBLIC_KEY="./keys/jwt_access_public.pem"
export JWT_ACCESS_PRIVATE_KEY="./keys/jwt_access_private.pem"

# Access token's expiry in minutes.
export JWT_ACCESS_EXPIRY=15
export JWT_REFRESH_PUBLIC_KEY="./keys/jwt_refresh_public.pem"
export JWT_REFRESH_PRIVATE_KEY="./keys/jwt_refresh_private.pem"
# Access token's expiry in minutes.
export JWT_REFRESH_EXPIRY=10080 # 7 days.
